services:
  gateway:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./configs:/app/configs
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
